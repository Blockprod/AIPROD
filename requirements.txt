# AIPROD monorepo — CORE dependencies (100% souverain — aucune dépendance SaaS)
accelerate>=1.2.0
aiohttp>=3.10.0
av>=13.0.0
bitsandbytes>=0.44.0
cachetools>=5.5.0
einops>=0.8.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
imageio>=2.36.0
librosa>=0.10.0
lpips>=0.1.4
matplotlib>=3.9.0
memory-profiler>=0.61.0
numpy>=1.26.0
opencv-python>=4.10.0
optimum-quanto>=0.2.0
pandas>=2.2.0
peft>=0.13.0
pillow>=11.0.0
pillow-heif>=0.18.0
pydantic>=2.10.0
pytest>=9.0.0
pyyaml>=6.0.0
rich>=13.9.0
safetensors>=0.4.0
scenedetect>=0.6.0
scipy>=1.14.0
torch>=2.5.0
torchaudio>=2.5.0
torchvision>=0.20.0
tqdm>=4.67.0
transformers>=4.47.0
typer>=0.15.0
zstandard>=0.23.0
xformers>=0.0.28

# Observabilité (auto-hébergeable)
prometheus-client>=0.21.0
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0
structlog>=24.4.0
mlflow>=2.18.0

# OPTIONNEL (installer séparément — souveraineté)
# pip install aiprod-cloud[all]       → toutes les intégrations cloud
# pip install aiprod-cloud[gcp]       → google-cloud-storage
# pip install aiprod-cloud[s3]        → boto3
# pip install aiprod-cloud[stripe]    → stripe
# pip install aiprod-cloud[huggingface] → huggingface-hub
# pip install aiprod-cloud[gemini]    → google-generativeai

# Windows environment note:
# - flash-attn: Not available on Windows (build system limitation). Use xformers instead.
# - triton: Not available on Windows (requires Linux/NVIDIA platform).
# - For GPU acceleration on Windows, xformers provides efficient attention implementations.
